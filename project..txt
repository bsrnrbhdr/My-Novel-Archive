sudo docker create -v /var/lib/postgresql/data --name MNAPostgresData alpine
sudo docker run -p 5432:5432 --name mna-postgres -e POSTGRES_PASSWORD=password -d --volumes-from MNAPostgresData  postgres:latest 
sudo docker network create mna-app-network --driver bridge
sudo docker network connect mna-app-network  mna-postgres
sudo docker inspect mna-postgres 
sudo docker start mna-postgres
sudo docker logs mna-postgres
sudo docker exec -it mna-server psql -U postgres
CREATE DATABASE mnadb;
\q

-------------------
pgadmin server oluşturdum çalıştı
docker start yaptıktan sonra https://stackoverflow.com/questions/43739438/how-to-fix-error-cannot-resolve-column
network icon sorunu lanet
First you have to stop network manager from background:

sudo service network-manager stop
Remove file on /var/lib/NetworkManager/NetworkManager.state path:

sudo rm /var/lib/NetworkManager/NetworkManager.state
Start network manager back:

sudo service network-manager start